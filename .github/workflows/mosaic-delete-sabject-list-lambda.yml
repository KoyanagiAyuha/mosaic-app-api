name: mosaic-dev-delete-sabject-list-lambda-deploy

on:
  push:
    branches:
      - master
    paths:
      - "mosaic-delete-sabject-list-lambda/**"
jobs:
  lambda-cd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-python@v1
        with:
          python-version: 3.9
      - run: cd mosaic-delete-sabject-list-lambda && pip install -r requirements.txt -t . && zip -r package.zip ./*
      - run: pip3 install awscli
      - run: cd mosaic-delete-sabject-list-lambda && aws lambda update-function-code --function-name mosaic-dev-delete-sabject-list-function --zip-file fileb://package.zip --publish
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: eu-central-1
